// DOM Ready
(function($, window, undefined) {
   $(function() {
      var globalsInit = function() {

         // =================================================================
            //Put all scripts that are to be fired on load here. If there becomes too many page specific scripts, then expand it to a "_page.js" file.
         // =================================================================

      };
      globalsInit();
   });
})(jQuery, window);
