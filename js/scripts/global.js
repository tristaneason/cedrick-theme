// DOM Ready
(function($, window, undefined) {
   $(function() {

      // =================================================================
         //Put all styles that are to be fired on load here. If there becomes too many page specific styles, then expand it to a "_page.js" file.
      // =================================================================


   });
})(jQuery, window);
